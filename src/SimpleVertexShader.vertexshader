#version 330 core

// Input vertex data, different for all executions of this shader.
layout(location = 0) in vec3 position;

out vec3 positionMod;

uniform float angle;

void main(){

	positionMod = position;

	gl_Position = vec4 (positionMod.x, positionMod.y, positionMod.z, 1.0) ;



	 
}